<UserControl x:Class="GakumasuCalc.Views.NumberSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root"
             Height="26" Width="64">
    <UserControl.Resources>
        <!-- 上下ボタン: フラットな小ボタン (押しっぱなしで連続増減できるよう RepeatButton) -->
        <Style x:Key="SpinButton" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="FontSize" Value="7"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="Background" Value="#F3F3F3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Delay" Value="400"/>
            <Setter Property="Interval" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#CFCFCF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="3" Background="White">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding Value, ElementName=Root, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}"
                     BorderThickness="0" Background="Transparent"
                     FontSize="13"
                     HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                     PreviewTextInput="OnPreviewTextInput"/>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="1"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <RepeatButton Grid.Row="0" Content="▲" Style="{StaticResource SpinButton}" Click="OnUpClick"/>
                <Border Grid.Row="1" Background="#DDDDDD"/>
                <RepeatButton Grid.Row="2" Content="▼" Style="{StaticResource SpinButton}" Click="OnDownClick"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
