## What's Changed

### Fixes
- **必須カード＋SP回数指定で編成が7枚に膨張する不具合を修正** (Web版/デスクトップ版)
  - 全属性のSP率カード（例：「食欲の秋なんです」）を必須指定し、SP回数を多め（例：Da2/Vi3）に設定すると、SP率カードが過剰に確保されて編成が6枚を超え（7枚になり）、レンタル枠が余分な非SP率カードに使われることがありました
  - 全属性のSP率カードはDa/Vi両方の必要枚数を同時に満たすものとして扱うよう補正し、編成が常に6枚に収まるようにしました
